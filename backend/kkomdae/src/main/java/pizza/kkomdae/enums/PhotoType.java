package pizza.kkomdae.enums;

public enum PhotoType {
    Front, FrontRightEdge, FrontLeftEdge, Rear, RearRightEdge, RearLeftEdge,
    Bottom, BottomRightEdge, BottomLeftEdge, Display, Keyboard, Else
    // TODO 프론트에서 오는 사진에 따라 수정
}
